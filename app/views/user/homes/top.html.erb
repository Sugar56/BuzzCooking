<div class="container" style="padding-top: 10px;">
  <div class="row">
    <div class="col-sm-3">
      <table class="table table-hover" style="background-color: #FFFFBB;">
        <thead>
          <tr align="center">
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td><%= link_to genre.name, genre_path(genre.id) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="col-sm-9">
      <div class="row" style="margin-bottom: 50px;">
        <div class="col bg">
          <p style="text-align: center; background-color: rgba( 0, 0, 0, 0.5); color: #fff; padding: 50px;">
            ようこそ、BuzzCookingへ！</br>
            このサイトは、料理レシピのECサイトになります。</br>
            ゲストの方でも投稿レシピの閲覧はできますが、</br>
            コメント機能などのご利用にはユーザー登録が必要になります。
          </p>
        </div>
      </div>

      <div class="row" style="margin-bottom: 10px;">
        <div class="col">
          <h2><strong><span style="background-color: #FFDBC9;">新着レシピ</span></strong></h2>
        </div>
        <div class="col" style="text-align:right;">
          <%= link_to '全ての投稿を見る >', recipes_path %>
        </div>
      </div>

      <div class="row">
        <div class="col">
          <div class="d-flex flex-row">
            <% @recipes.each do |recipe| %>
            <div class="d-flex flex-column bd-highlight" style="margin-right: 10px; margin-bottom: 50px;">
              <%= image_tag recipe.get_image, size: "200x200" %>
              <p><strong><%= link_to recipe.name, recipe_path(recipe.id) %></strong></p>
              <%= recipe.recipe_explanation %>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
